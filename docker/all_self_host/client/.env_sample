# docker/all_self_host/client/.env として保存する（別PC側）
# Raspberry Pi の LAN アドレス、または WireGuard 等 VPN 上のアドレス
MC_SERVER_HOST=192.168.1.50

# Grafana をこのPC以外からも開くなら 0.0.0.0 に変更する
GRAFANA_BIND_IP=127.0.0.1

GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=change-me
